#include "main.h"

/**
 * mul - Main Function
 * @a: The first number
 * @b: The second number
 *
 * Description: This function multiplies two numbers
 *
 * Return: 0(if not digit) and 1 (if it is a digit)
 */

int mul(int a, int b)
{
	return (a * b);
}
